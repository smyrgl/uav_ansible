---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Reload udev
  ansible.builtin.command:
    cmd: udevadm control --reload-rules
  changed_when: true

- name: Restart gpsd
  ansible.builtin.systemd:
    name: gpsd
    state: restarted
